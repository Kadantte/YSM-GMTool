namespace App.Desktop.ViewModels;

/// <summary>Which field the search box queries against. Mirrors the WinForms SearchMode.</summary>
public enum SearchMode
{
    ById,
    ByName,
    ByContactScript,
}
